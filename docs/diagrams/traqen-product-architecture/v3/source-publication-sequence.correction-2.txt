Change only the endpoint of message 2 绑定操作、候选修订与持有者: its arrow starts at C06 and MUST END at the FAR-RIGHT PostgreSQL lifeline, not at the 专用文件卷 lifeline. Extend that horizontal line all the way to PostgreSQL and put its sole arrowhead there. Message 3 still ends at 专用文件卷. Also extend the recovery-frame 查询未发布 / 读取原结果 horizontal arrows to end exactly at PostgreSQL's far-right lifeline, with notes moved below if necessary; these query records from PostgreSQL. Preserve all other labels, four participants, transaction annotation, separate F002 phase, and footers. No other modifications.
