Correct this A1 application diagram without changing its eight module identities or their responsibility labels. Three edges currently have wrong endpoints. First delete HTTP入口->M03 I03 arrow. Draw HTTP入口 -> M04 基线治理 labelled "I03 人工审阅", routed down the clear gap left of M03, never through M03. Second delete M02 -> M04 I02 持久证据. Draw M02 -> M03 labelled "I02 持久证据", routed upward in the gap between left and center columns; it must end on M03 and not on M04. Third delete the bidirectional dashed I06 edge joining M05 and M08. Replace it with exact small keyed reference in M08 "I06 → M03：固定运行上下文" and in M03 "I06 来自 M08"; no crossing arrow across M05. Delete the outside box "任务启动 / 查询" and its connecting line to Runner/CI; retain the independent Runner/CI -> M05 I04 ingestion edge. All correct edges remain: M01->M02 I01, M03->M04 candidate refs, M02->M06 I02, M04->M06 approved claims, M05->M06 execution, M02/M04/M06->M07 projections. Replace bottom adapter strip text and arrows with "应用用例协调领域规则与存储端口；适配器实现端口 → PostgreSQL / 文件卷". Keep the footer constraints. All orthogonal edges must terminate at the correct module, with no arrow passing through another node.
