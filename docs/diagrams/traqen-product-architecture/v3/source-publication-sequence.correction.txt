Correct the sequence diagram to remove inaccurate lifeline endpoints and to distinguish failure scenarios from a successful run. Keep title and first four participants: 授权用户/工作台, C06发布用例, 专用文件卷, PostgreSQL. REMOVE the fifth C07/F002 header and its lifeline entirely; admission will be a separate bottom phase. Main message 3 arrow must END EXACTLY at 专用文件卷 dashed lifeline; its 字节已持久化 return must START EXACTLY there and end at C06. Other main message endpoints stay correct. Replace the wide green atomic box spanning file volume with a green annotation located at the PostgreSQL lifeline ONLY saying "PG 事务内原子发布：Bundle / Receipt / 操作结果"; keep the COMMIT response PostgreSQL->C06. Rename the lower alt frame title to "独立故障场景（替代相应正常步骤）", not UML alt after successful commit. Rename first guard "[步骤4事务失败]" and second "[步骤5响应丢失]"; retain their existing recovery messages and notes. Delete the entire bottom incorrect admission request/check arrows and any stray lifeline below the failure scenarios. Replace that bottom region with ONE separated flow strip titled "另一个显式动作：启动 F002", containing four boxes linked left-to-right: "用户明确启动" -> "C07 当前准入" -> "复验记录与内容" -> "通过：I01 / 不通过：拒绝". Below that strip text "权限撤回、内容损坏、接受过期拒绝新准入；旧历史保留". Keep final footer about finalisation cancellation/dedup/renewal. All four main lifelines stop above the separate F002 strip. No transaction enclosure includes the file volume.
