Create one rigorous software architecture diagram, landscape 3:2 canvas, minimum 1536x1024, crisp vector-like raster, white background, navy text, thin orthogonal connector lines, restrained teal/blue accents, amber only for gaps/proposals, generous spacing. Chinese headings with exact short English identifiers where specified. Clean architectural drafting inspired by Archify, no decorative illustrations, no gradients or 3D. Every arrow must have the stated meaning and correct direction. Text must be readable. Do not invent nodes or connections. Do not reproduce the old generic L1-L5 horizontal layer stack. Reserve 7% margins. A3 title "Traqen · 来源发布与故障恢复". Subtitle "下游可见性原子提交｜文件系统与 PostgreSQL 不共享事务". This must be a UML-style SEQUENCE DIAGRAM with vertical dashed lifelines and numbered horizontal messages, not boxes arranged in layers. Five lifelines left to right "授权用户 / 工作台", "C06 发布用例", "专用文件卷", "PostgreSQL", "C07 / F002". Main sequence top to bottom: 1 user->C06 "明确冻结请求"; 2 C06->PostgreSQL "绑定操作、候选修订与持有者"; 3 C06->volume "校验、持久化字节、保护引用"; dashed volume->C06 "字节已持久化"; 4 C06->PostgreSQL "最终事务：复验权限 / 接受期限 / 修订"; draw green frame spanning C06 and database showing "原子发布 Bundle + Receipt + 操作结果"; database->C06 dashed "COMMIT"; 5 C06->user dashed "返回已发布结果". Under main sequence put a UML alt frame with two alternatives each occupying one clear row: amber "[文件完成，事务失败]" C06->PostgreSQL "确认未发布" note "私有准备内容保留并核验复用；无可消费 Receipt"; blue "[事务成功，响应丢失]" user->C06 "按操作查询 / 重试", C06->PostgreSQL "读取原结果", C06->user "同一 Bundle / Receipt + 当前资格". Final visually separated phase "另一个显式动作：启动分析" user->C07/F002 "请求分析"; C07/F002->PostgreSQL "重验权限、完整性、接受期限"; last note "通过才取得 I01；过期 / 损坏 / 撤权则拒绝新准入，历史保留". Footer "进入最终化后不接受取消；重复请求不新建版本；续签是新操作、新 Receipt，同一 Bundle。". No distributed transaction symbol joining volume/database, no auto-call from COMMIT to F002. Prefer black text and sparse accent colors; fit readable labels.
