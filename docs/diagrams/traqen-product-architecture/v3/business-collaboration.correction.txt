Edit this existing B2 business swimlane diagram. Keep all ten nodes, lanes, title, main solid arrows and the right-hand risk-acceptance J08->J09 dashed arrow exactly as they are. Correct ONLY the three inaccurate return paths using explicit numbered cross-references, to remove all misleading arrowheads. (1) Delete the blue dashed arrow labelled 补证 between J02 and J03 entirely; it currently points the wrong way. Add clear small text inside/beside J03: "补证：返回 J02". (2) Delete the amber dashed horizontal arrow labelled 补证 / 修复 from J10 toward J04 entirely. Under the J08 box in the shared responsibility panel add "补证 / 修复：返回 J04" and "复验：返回 J05". Keep the separate orange J08->J09 path with label 已接受风险仍可见. (3) Delete the blue dashed return arrow labelled 复核与复验 that currently starts at J07 and points J05; it is wrong. Add two short lines inside/beneath J10: "复核 J04 / 复验 J05". Use adequate spacing and do not overlap the vertical J09->J10 line. Bottom footer add a short legend "编号引用表示返回步骤". Preserve every other correct main connector, especially J02->J03 候选与依据, J03->J04 批准, J07->J08 有 and J07->J09 无待处置项. The result is a rigorous readable process diagram with explicitly indexed return paths.
