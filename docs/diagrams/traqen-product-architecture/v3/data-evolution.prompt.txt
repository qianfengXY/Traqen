Create one rigorous software architecture diagram, landscape 3:2 canvas, minimum 1536x1024, crisp vector-like raster, white background, navy text, thin orthogonal connector lines, restrained teal/blue accents, amber only for gaps/proposals, generous spacing. Chinese headings with exact short English identifiers where specified. Clean architectural drafting inspired by Archify, no decorative illustrations, no gradients or 3D. Every arrow must have the stated meaning and correct direction. Text must be readable. Do not invent nodes or connections. Do not reproduce the old generic L1-L5 horizontal layer stack. Reserve 7% margins. D2 title "Traqen · 数据版本与状态演化". Subtitle "历史事实、当前资格、人工权威和验证结果分别表达". Create a time-oriented diagram, left-to-right chronological columns "t0 已有依据", "t1 发生变化", "t2 明确处置 / 新证据". Four horizontal lanes each with explicitly different data semantics, no vertical arrows equating states. Lane E01 "来源资格": t0 "Bundle B + Receipt R1 可准入"; t1 "R1 接受过期：禁止新分析"; t2 "重新接受 → Receipt R2". A long thin immutable bar below these boxes "Bundle B 内容不变；R1 历史保留；R2 是新签发记录". Lane E02 "业务权威": t0 "Feature F / 规则版本 V1"; t1 "业务规则修订候选"; t2 "人工 Decision → 新版本 V2". A persistent line "Feature F 身份不变；代码变化不自动改写业务意图". Lane E03 "实现与验证": t0 "Snapshot S1 / Execution X1 / 结果"; t1 "Snapshot S2 → 受影响映射待复核"; t2 "新 Execution X2 → 新验证结果". Annotation "旧 X1 保留原版本；未复验不沿用旧 PASS". Lane E04 "只读投影": t0 "API / 业务 / 质量视图"; t1 "显式展示缺口、冲突、陈旧"; t2 "从确切版本记录重建". Bottom separated legend six independent status badges "权威" "符合性" "验证" "新鲜度" "冲突" "覆盖". Footer "同一包的新 Receipt 可有不同备份覆盖；历史、任务、审计默认 TTL=0。风险接受不等于验证通过。". Temporal arrows only within each lane and labelled "过期", "人工修订", "新快照", "重建" as appropriate. This is an evolution model, NOT a universal linear state machine; show four lanes separately.
