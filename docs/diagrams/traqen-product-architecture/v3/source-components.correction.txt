Correct the source component diagram. Preserve all C01-C09 and M01/M02 identities. Fix exact relationships: (1) Change the label on C01->C04 from 预检约束 to 已验证材料. (2) C02 currently has two edges to C04. Keep only one labelled 已验证字节; delete the extra 已验证材料 edge from C02. Add inside C02 a small explicit reference "预检约束来自 C03" instead of a crossing line. (3) Delete the C04->C06 edge labelled 两份已冻结 manifest. Delete the C06->C08 edge. Add inside C08 "输入：C04 两份已冻结 manifest"; this is an explicit indexed input relationship, not publication triggering comparison. (4) Delete the dashed bidirectional C06/C07 line. In C07 add "读取 C06 已发布记录"; this describes visibility, not a call that starts F002. (5) Delete the arrow from C05 to the dedicated volume cylinder. Label that volume with a small reference "由 C04 管理". C05 produces PostgreSQL metadata, NOT file blobs. (6) Make the C06->C04 protection arrow ONE WAY with arrowhead at C04 only. Keep C05->C06 对账结果 and C06->PostgreSQL publishing. Keep external Git->C01, upload->C02, C04->C05, explicit human->C06, M02->C07 admission request and C07->M02 I01. Footer add "编号引用说明跨区域读写；不表示自动触发". Preserve clear white spacing.
