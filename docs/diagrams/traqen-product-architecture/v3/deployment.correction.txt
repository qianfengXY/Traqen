Correct the deployment diagram's critical network endpoints. Keep nodes P01-P10, boundaries, target/current labels and backups. (1) The amber dashed 受策略约束的出站 arrow currently points from P04 CLI to P05 PostgreSQL, which is WRONG. DELETE that dashed orange/blue path entirely. Draw ONE dashed orange arrow from the RIGHT border of P04 CLI upwards through clear space at the far right of the application-node boundary, ending EXACTLY at the BOTTOM border of P10 CLI供应商. Label it 受策略约束的出站. No CLI arrow to P05. (2) The blue 事务记录 arrow currently starts in empty space beside P06. DELETE it. Draw an elbow bidirectional P03 Node API <-> P05 PostgreSQL line, starting exactly at P03's RIGHT-LOWER border, routed below P04, and ending at P05's LEFT border. It must not attach to P06. (3) Add short label under the two inbound Git/Runner arrows "外部输入经 P03" to clarify they enter the API inside the node. (4) Fix title to "P1 Traqen · 部署架构". Replace clipped subtitle with "设计目标与运行基础（尚未验收）". Label P06->P07 backup path "受保护引用集合". Preserve P05->P07 一致性数据库备份. All unrelated content unchanged; ample whitespace for correct edge routing.
