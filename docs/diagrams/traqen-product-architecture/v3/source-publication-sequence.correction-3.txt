Edit this diagram by simplifying ONLY the middle fault-scenarios frame. Keep the normal sequence above and the separate F002 flow below unchanged. Inside the frame 独立故障场景（替代相应正常步骤）, ERASE ALL lifeline segments, horizontal arrow lines, arrowheads, and old text except the frame heading and middle dashed horizontal row separator. It must now contain TWO TEXT-ONLY scenario rows, no arrows and no lifelines. First row text: '步骤4事务失败：C06 查询 PostgreSQL 确认未发布；保留并复验私有准备内容，无可消费 Receipt。' Second row text: '步骤5响应丢失：C06 从 PostgreSQL 读取原操作结果；返回同一 Bundle / Receipt 与当前资格。' Allow these sentences to wrap into two lines within their respective rows. Keep the outer fault frame and its heading. The fault rows are independent alternatives, not steps after success. Do not change anything outside this frame.
