INITIAL GENERATION PROMPT

Generate an original whole-page enterprise TECHNICAL architecture diagram for Traqen. Use supplied image ONLY as a STYLE reference, same white background, navy Chinese title, clear horizontal L1-L5 layer bands, teal compute, purple durable storage, amber constraints, clean flat design, no 3D. Landscape 3:2 high resolution. Title “Traqen 技术架构 · 分层详图”. Subtitle “V2 对比稿 | 模块化应用、有界执行、持久化与恢复”. EXACTLY 18 unique numbered nodes T01-T18. Legible simplified Chinese labels, enough whitespace, no arbitrary protocol labels, no invented vendors, no Redis, no Kubernetes, no direct model API.
MAIN area five horizontal layers, THREE cards per layer T01-T15; narrow RIGHT SIDE crosscutting rail T16-T18.
L1 “交互与请求入口”
T01 Web 工作台 — React / TypeScript / 交互与展示
T02 应用 API — 请求校验 / Workspace 上下文
T03 长任务状态查询 — 服务端进度 / 等待原因 / 恢复入口
Use T01→T02 arrow labelled “请求”; no arrow T03→data invented. Layer note “交互请求不承载长时间采集和模型执行”.
L2 “应用与领域”
T04 应用用例服务 — 采集 / 分析 / 审阅 / 影响
T05 领域规则 — 身份版本 / 授权 / 发布 / 失效
T06 Repository 边界 — 来源真相 / 记录与关系 / 内容引用
Use T04→T05 “规则” and T04→T06 “持久化接口” as textual relation strip if line clutter occurs. Logical modules, not separate microservices.
L3 “任务与执行适配”
T07 持久化任务协调 — 排队 / 检查点 / 重试恢复
T08 有界 Worker — 采集 / 提取 / 模型分析
T09 CLI 与 Runner 适配 — 白名单参数 / 权限 / 结果校验
One simple solid rail T07→T08 “派发”, T08→T09 “受控调用”. Note “CLI 使用固定 Run；Runner 或 CI 提供真实执行证据”.
L4 “持久化与内容”
T10 PostgreSQL — 清单 / 任务 / 版本 / 决策 / 执行
T11 专用持久化卷 — 不可变来源字节 / 内容摘要
T12 内容引用与发布 — 字节持久化 / 引用保护 / 事务发布
Make NO arrow pretending DB and files share a transaction. In layer note exact “先持久化并保护字节引用，再事务发布记录”.
L5 “恢复与运行保障”
T13 配套备份 — 数据库 + 被引用字节
T14 隔离恢复与校验 — 恢复记录 / 校验内容完整性
T15 容量与运行诊断 — 磁盘与队列 / 日志 / 恢复动作
Arrow T13→T14 “恢复验证”. Layer note “首期单节点；容量不足阻止新写入，保留既有历史”.
RIGHT VERTICAL CROSSCUTTING RAIL “全层约束”
T16 Workspace 访问控制 — 文件 / 网络 / 工具 / 凭据
T17 配置固定与出站策略 — 非敏感快照 / Secret 引用
T18 持久化与审计规则 — TTL=0 / 操作历史 / 可追溯
Plain bracket connectors across layers; no arrows suggesting access policies are services.
At bottom, a readable exact relationship strip “关键技术交接”:
“T02 → T04；T04 → T07；T08 经 T06 访问持久化”
“T09 调用获授权 CLI 或对接 Runner；执行结果校验后入库”
“T10 + T11 → T13 → T14；文件与数据库不是同一事务”
Keep cross-layer dependencies in this strip instead of uncontrolled criss-cross arrows. Layer ordering is logical responsibility, not mandatory execution sequence.
Footer: “建议技术职责分层；不代表进程隔离已落地 | 任务进度 / 内容冻结 / 当前准入 / 备份覆盖 分别表达”.
Do not say high availability is implemented, no fake performance numbers. Every ID should remain legible and belong to its specified layer.

CORRECTION PROMPT

Edit the supplied TECHNICAL architecture image while preserving the entire layout, ALL T01–T18 nodes, colors, layers, clear Chinese text, and footer. Correct one inaccurate dependency: REMOVE the arrow from T05 领域规则 to T06 Repository 边界, including the “持久化接口” label in that gap. Keep the T04→T05 arrow labelled 规则. Replace the current L2 band note “逻辑模块，非独立微服务” with EXACT “T04 使用 T05 规则与 T06 接口；逻辑模块不等于独立微服务”. This explicitly represents both dependencies without implying that domain rules alone own repository calls. Do not draw a new arrow from T05 to T06. Everything else must remain unchanged, especially T07→T08→T09, source/record boundaries, T10+T11→T13→T14 in the bottom relation strip, and the T16–T18 crosscutting rail. Preserve typography and full-page proportions. No omitted or duplicate node IDs.
